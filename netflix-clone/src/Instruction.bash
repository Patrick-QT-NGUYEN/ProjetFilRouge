## Créeation d'un projet React 
### creer un dossier  => dans un dossier taper la commende " npx create-react-app + nom du projet = creation du dossier avec le nom du projet 
###et contenant tout les sous dossier React et Reactjs
# dans le terminal 
npm install installe

npx create-react-app premierproject // npx create-react-app <Nom du project >

cd premierproject  /// cd <Nom du project >

npm start

si probleme // npm install
##création de dossier
dans dossier rsc ==> create dossier Asset 

##instaler  extantion studio code
Simple React Snippets
Reactjs code snippets

# Toujours ajouter "--save " pour sauvegarder news modules
npm i ou instaler " nom du module a installé  " --save

## install un routeur
npm install react-router-dom --save
npm install react-router-dom@6
configure le routeur

#install dependencies
npm install --save-dev @testing-library/user-event
npm install --save-dev @testing-library/react
npm install --save-dev @testing-library/jest-dom

## Install Material Icons
npm install @mui/icons-material @mui/material @emotion/styled @emotion/react

## Install Bootstrap Icons
npm i bootstrap-icons
#install bootstrap
npm install react-bootstrap bootstrap@5.1.3 --save
a mettre dans un fichier parent (index.js)
import 'bootstrap/dist/css/bootstrap.min.css';

## Install Axios
npm install axios --save

## install SCSS
npm i SCSS --save

## Install JSX
npm install --save react-jsx

## info utile
Inside that directory, you can run several commands:

  npm start
    Starts the development server.

  npm run build
    Bundles the app into static files for production.
     Regroupe l'application dans des fichiers statiques pour la production.'

  npm test
    Starts the test runner.
      Démarre le testeur.

  npm run eject
    Removes this tool and copies build dependencies, configuration files
    and scripts into the app directory. If you do this, you can’t go back!
     Supprime cet outil et copie les dépendances de construction, les fichiers de configuration
     et les scripts dans le répertoire de l'application. Si vous faites cela, vous ne pouvez pas revenir en arrière !'

We suggest that you begin by typing:

  cd execerce1
  npm start